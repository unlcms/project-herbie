<figure data-node="node-{{ node.id }}">
  <div class="artimage">{{ content.artwork_image }}</div>
  <figcaption class="tombstone">
    <span class="tombstone-artist">{{ content.artist }}, </span>
    <span class="tombstone-title"><em>{{ content.artwork_title }}</em>.</span>
  </figcaption>
</figure>

<style>
/* replacement for .art-image */
.artimage a {
  display: block;
  position: relative;
  width: 100%;
  height: 0;
  overflow: hidden;
  padding-bottom: 62.5%;
  background-color: #fbfbfb;
  border: 1px solid #e6e7e8;
  transition: border 0.5s ease-out;
}
.artimage a:link {
  border: 1px solid #e6e7e8;
}
.artimage img {
  display: block;
  position: absolute;
  top: 0;
  bottom: 0;
  left: 0;
  right: 0;
  height: initial;
  width: auto;
  max-width: 100%;
  margin: auto;
}
</style>
